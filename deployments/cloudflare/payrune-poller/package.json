{
  "name": "payrune-poller",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "bash ../../../scripts/build-cf-poller-worker-wasm.sh",
    "check": "npm run build:wasm && node --check src/index.mjs && node --check src/go-wasm-runtime.mjs && node --check src/postgres-bridge.mjs && node --check src/bitcoin-observer-bridge.mjs",
    "test": "npm run build:wasm && node --test test/*.test.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "wrangler": "^4.6.0"
  }
}
